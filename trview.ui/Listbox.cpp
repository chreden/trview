#include "Listbox.h"

namespace trview
{
    namespace ui
    {
        Listbox::Listbox(const Point& position, const Size& size)
            : Control(position, size)
        {
        }

        Listbox::~Listbox()
        {
        }
    }
}
