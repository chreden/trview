#pragma once

namespace trview
{
    namespace ui
    {
        enum class SizeMode
        {
            // The control will not resize when the text is changed.
            Manual,
            // The control will resize when the text is changed.
            Auto
        };
    }
}