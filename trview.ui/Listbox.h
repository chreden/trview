#pragma once

#include "Control.h"

namespace trview
{
    namespace ui
    {
        class Listbox : public Control
        {
        public:
            Listbox(const Point& position, const Size& size);

            virtual ~Listbox();
        private:

        };
    }
}
