#include "NumericUpDown.h"

namespace trview
{
    namespace ui
    {
        NumericUpDown::NumericUpDown(Point point, Size size)
            : Window(point, size, Colour(1.f, 1.f, 1.f, 1.f))
        {
        }
    }
}
